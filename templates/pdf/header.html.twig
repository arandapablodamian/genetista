<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<link href="https://fonts.googleapis.com/css?family=Lato:300,400,700,900" rel="stylesheet">
		<link rel="preconnect" href="https://fonts.gstatic.com">
		<link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet">
		<link rel="preconnect" href="https://fonts.gstatic.com">
		<link href="https://fonts.googleapis.com/css2?family=Quicksand&display=swap" rel="stylesheet">
		<style>
			
			.header-left {
				display: inline-block;
				text-align: left;
				width: 400px;
			}
			.header-right {
				text-align: right;
				float: right;
			}

			.img-logo-dotto{
				height:50px;
				display: inline-block;
			}

			.img-logo-client{
				height:50px;
				display: inline-block;
				margin-left:5px;
			}
			
			.text-client {
				font-family: 'Lato', sans-serif;
				font-size: 24px;
				color: #353535;
				font-weight: bold;
			}

		</style>
	</head>
	<body>
        <div style="padding:25px 50px;">
            <div class="header-left">
                {% if result_report.reportLogo %}
                    {% if  result_report.reportLogo.showLogoJD %}
                        <img class="img-logo-dotto" src="{{absolute_url(asset('assets/img/LogoCentrodeGenéticaJorgeDotto_-11.png'))}}" alt="" alt="">
                    {% endif %}
                {% else %}
                    <img class="img-logo-dotto" src="{{absolute_url(asset('assets/img/LogoCentrodeGenéticaJorgeDotto_-11.png'))}}" alt="" alt="">
				{% endif %}
				{# {% if result_report.reportLogo %}
                    {% if  result_report.reportLogo.path %}
                        <img style="height:50px;" src="{{absolute_url(asset('/uploads/section/images/'~ result_report.reportLogo.path))}}" alt="" alt="">
                    {% endif %}
                {% endif %} #}
            </div>
            <div class="header-right">
                <div class="text-client">
					{{result_report.client}}
				</div>
				<div style="margin: 0px auto;display:block">
				{% if result_report.reportLogo %}
                    {% if  result_report.reportLogo.path %}
                        <img style="height:50px;margin: 0px auto;display:block" src="{{absolute_url(asset('/uploads/section/images/'~ result_report.reportLogo.path))}}" alt="" alt="">
                    {% endif %}
                {% endif %}				
				</div>
            </div>
        </div>
    </body>
</html>